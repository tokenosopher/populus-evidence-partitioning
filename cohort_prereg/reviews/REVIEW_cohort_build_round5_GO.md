Referee ruling: GO — FREEZE CLEARED

Based on the Round-5 implementation evidence, all outstanding Round-4 deltas are closed. I find no remaining scientific-design, decision-machinery, mechanism-audit, seed-integrity, or admission-test objection to freezing this revision.

1. Noise construction — RATIFIED

The specification is sufficiently literal and reproducible:

versioned, case-keyed domain separator;

fixed SHA-256-to-seed conversion;

standard-normal CPU float32 direction;

Frobenius-norm matching per intact recipient packet;

cast only after scaling;

explicit zero-norm behavior and counts;

hard failure on zero or non-finite sampled directions;

deliberate reuse of the same direction across recipients.

Omitting recipient identity from the key is acceptable because it is declared in advance and produces a shared perturbation direction for cross-recipient comparability rather than model-dependent noise selection. Scaling to each recipient’s native packet norm preserves the intended destructive control.

This remains properly distinct from deletion, derangement, and semantic packet transplants, as in the parent mechanism battery. 

populus_arxiv_v3_final

2. Counterfactual span rewrite — RATIFIED

The revised control is now mathematically matched to the packet intervention.

For each interface k, computing

v
k
cf
	​

=F
k
	​

(x)+3(mod17)

and then

x
k
cf
	​

=F
k
−1
	​

(v
k
cf
	​

)

ensures that the native rewritten episode reaches exactly the same counterfactual intermediate state as the +3 packet transplant. The two runtime assertions close the important failure modes:

F
k
	​

(x
k
cf
	​

)=v
k
cf
	​


and

rewritten final target=suffix-implied packet target.

Removing the old unconditional x↦x+3 variant avoids an unnecessary extra condition and prevents it from being mistaken for the matched text control.

3. Dialect indicators — RATIFIED

The amended definitions are safe as descriptive, non-gating operational indicators:

strong_within_regime_canonicality: self-same and self-counterfactual fidelity both at least 0.90;

partial: both at least 0.60, without satisfying the strong criterion;

operational_context_entanglement: self-same below 0.60 and deletion accuracy at most 0.70 on the common success-conditioned set;

cross_regime_interoperability: the complete raw-performance and normalized-ratio conjunction in both donor directions;

fewer than 30 eligible cases: raw metrics only, indicators set to null.

Renaming the former “exact” category was necessary and correct. The interoperability definition now prevents a favorable ratio from disguising uniformly poor absolute transfer. The low-denominator rule also prevents absence of evidence from becoming a negative phenotype.

The context-entanglement label is adequately bounded by the explicit statement that it does not uniquely identify the underlying mechanism.

4. Development-exposure blocker — CLOSED

The role-aware complete-bundle rejection is now the correct implementation:

every seed is checked against the exposure set for its own semantic role;

order and initialization arrays are checked elementwise;

synthetic world identifiers are checked separately;

the entire candidate bundle is rejected;

no individual field is replaced opportunistically;

the candidate counter advances deterministically;

the exposure manifest is the single source of truth.

The regression showing that the same integer is rejected in its exposed role but not in an unrelated role is particularly useful. It demonstrates that the system is neither under-inclusive nor indiscriminately over-inclusive.

5. Mechanism/dialect blocker — CLOSED

The previously missing elements are now present:

norm-matched noise;

mathematically matched span rewrite;

donor-coverage intersection before matrix execution;

identical eligible recipient case sets across self and cross donors;

case-set hash equality as a hard assertion;

complete four-cell donor–recipient matrix coordination;

preserved pre-intersection coverage provenance;

raw numerators and denominators;

separate preservation and counterfactual ratios;

undefined zero-denominator handling;

non-exclusive descriptive indicators;

explicit LOW_DENOMINATOR;

explicit MATRIX_NOT_VIABLE that does not affect primary verdicts or trigger replacement training.

That satisfies both the self-mechanism and cross-dialect specifications.

6. Reader-path admission blocker — CLOSED

The new test correctly removes the confound that motivated the previous objection. In the parent architecture,

P
out
	​

=RMSNorm(P
in
	​

+0.05ΔP),

so testing only final-packet sensitivity could never establish that the Transformer actually read the incoming pseudo-tokens. Capturing and testing ΔP before the mailbox anchor directly exercises the learned reader–Transformer–writer path. 

populus_arxiv_v3_final

The completed admission battery now jointly verifies:

cell-0 and downstream restricted foreign invariance;

own-span sensitivity at ΔP;

global foreign-span sensitivity at ΔP;

incoming-packet sensitivity at downstream ΔP;

question readability in every cell call;

packet pseudo-token readability only at mail-receiving stages;

exact agreement between the captured restricted span mask and frozen layout mask;

mouth question readability.

That is a proper end-to-end implementation test of the intended information boundaries.

7. Smaller Round-4 clarifications — CLOSED

The following are also adequately resolved:

every primary operator is asserted to belong to singleton-training support;

tag-permutation denominators are part of the frozen first-valid-world criterion and are compiler-verified;

the filler PRF now has an allow-listed nonsemantic stream namespace;

N+ schedule changes leave the semantic stream identical across all conditions;

ADMISSION_PREFIX3 is located at an exact optimizer-update and accumulation boundary;

the state digest covers the relevant trainable, optimizer, and RNG state;

absent scheduler, scaler, and NumPy training RNG are declared rather than silently omitted;

verdict inputs are validated before the production lock is created;

the full pre-intersection and intersection provenance is retained;

newly implemented corrections introduced no unledgered development-world exposure.

Freeze authorization

The scientific-content freeze ceremony may proceed in this order:

finalize the literal hash inventory and remove or resolve every remaining scientific-content placeholder;

commit the exact preregistration, implementation, tests, configurations, schemas, exposure manifest, tier declaration, and protocol history;

deposit that exact revision in the public evidence repository;

obtain an immutable, retrievable Software Heritage record tied to the precise scientific-content commit;

record its SWHID and timestamp through the already specified metadata-only pointer mechanism, without modifying the frozen scientific-content bytes;

only then generate the fresh six-family seed bundle through the frozen role-aware rule;

generate and archive the populated pre-run manifest;

perform the two-box admission procedure and replay the mask, stream, serialization, and training-state fingerprint checks;

launch the 60-society GOLD fleet only after admission passes.

Any mismatch between the reported implementation and the archived bytes, any unresolved digest, or any seed generation preceding the immutable content timestamp would void this clearance. Those are the already registered mechanical conditions of freeze, not new scientific amendments.

I have reviewed the supplied completion evidence and the parent paper, but I have not independently executed the repository in this consult. The certification therefore attaches to the described revision and its hash-matching archived implementation.

Certification for the record

Standing independent referee determination: The Round-5 role-marked cohort revision closes all outstanding pre-freeze objections. The noise intervention, interface-matched counterfactual span control, dialect indicators, role-aware development-exposure denial, complete mechanism/dialect matrix, and downstream packet-reader admission test are approved. No further conceptual, methodological, gate, audit, statistical, or integrity edit is required before scientific-content freeze. The freeze ceremony is authorized, conditional on literal resolution of all freeze fields, byte-level agreement of archived artifacts with their declared digests and reviewed specifications, and establishment of an immutable retrievable scientific-content timestamp before any seed generation.

FINAL VERDICT: GO — FREEZE APPROVED
